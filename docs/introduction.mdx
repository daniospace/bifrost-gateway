---
title: Welcome to AuraLLM
description: "AuraLLM: The Cloud-Native, FinOps-Enforced AI Gateway"
---

AuraLLM is a highly performant, commercial-grade AI Gateway written in Go. It serves as a secure, lightweight bridge connecting your developers' standard OpenAI codebases to any downstream LLM provider (OpenAI, Anthropic, Gemini, DeepSeek, LLaMA, etc.) while enforcing **real-time budget limits, local PII sanitization, and automated model migration audits** on-the-fly.

```python Python (From standard OpenAI Client to AuraLLM in 2 lines!)
import openai

client = openai.OpenAI(
    api_key="gw-team-key-your-secret-here", 
    base_url="https://gateway.yourcompany.com/v1" # Simply swap URL to get strict budget caps!
)
```

---

## 🏗️ System Architecture

AuraLLM sits seamlessly as a proxy between your internal developers and public cloud LLM servers. Because Mintlify natively supports **Mermaid.js**, you can view our live-rendering network architecture below:

```mermaid
graph TD
    subgraph Client Network (Your VPC)
        A[Developer Apps / SDKs] -- "1. Standard API Request" --> B(AuraLLM Proxy Server)
        B -- "2. Check Key & Budget" --> C{Redis Cache Sync}
        C -- "Hit (Safe)" --> D[PII Regex Scrubber]
        C -- "Over Budget (Blocked)" --> B
        D -- "3. Local Prompt Redaction" --> E[Upstream Gateway Router]
    end

    subgraph Public Cloud Providers
        E -- "4. Hashed Safe Prompts" --> F[OpenAI API]
        E -- "4. Hashed Safe Prompts" --> G[Anthropic API]
        E -- "4. Hashed Safe Prompts" --> H[DeepSeek API]
    end

    subgraph Background Analytics
        F --> I(Translate Response)
        G --> I
        H --> I
        I -- "5. Re-inject Secrets Locally" --> B
        I -- "6. Asynchronous Logging" --> J[(PostgreSQL Database)]
        I -- "7. Budget Alerting" --> K[Slack / Webhooks]
    end

    style B fill:#059669,stroke:#10b981,stroke-width:2px,color:#ffffff
    style C fill:#1e1b4b,stroke:#4f46e5,stroke-width:1px,color:#ffffff
    style D fill:#064e3b,stroke:#10b981,stroke-width:1px,color:#ffffff
    style J fill:#0f172a,stroke:#334155,stroke-width:1px,color:#ffffff
```

---

## ⚡ Feature Comparison Matrix

When pitching to stakeholders, AuraLLM stands out by packing enterprise privacy and migration guardrails directly into the gateway proxy layer, bypassing the need for separate security vendor integrations:

| Capability | LiteLLM (Python) | Portkey (Node) | AuraLLM (Go) |
| :--- | :---: | :---: | :---: |
| **Runtime Overhead** | High (Python GIL) | Medium (Node) | **Ultra-Low (<1ms added)** |
| **Idle Memory Footprint** | ~200MB - 300MB | ~120MB | **~15MB** |
| **Binary Size / Dependencies** | Giant Python runtime / Venv | Complex Node dependencies | **None (Single 20MB Go Binary)** |
| **Distributed Budget Sync** | Complex manual setup | Cloud SaaS only | **Built-in Atomic Redis Sync** |
| **Local PII & Secret Redaction** | ❌ None (No-op) | ❌ None (No-op) | **✓ Built-in Local Scrubber** |
| **Non-Blocking Shadow Auditing** | ❌ None (No-op) | ❌ None (No-op) | **✓ Built-in Migration Auditor** |
| **Single-Sign-On (SSO) Portal** | ❌ Enterprise paid only | Cloud SaaS only | **✓ Built-in Okta OIDC Gate** |
| **Budgets Webhook Alerts** | ❌ Paid only | Cloud SaaS only | **✓ Built-in Slack Webhooks** |

---

## ⚡ Why Mid-Sized Teams Choose AuraLLM

Mid-sized engineering organizations (100 to 1,000 employees) adopt AuraLLM to satisfy budget predictability and private data compliance without the overhead of heavy enterprise security middleware:

*   **Distributed Budget caps:** Atomic budget deduction synchronized globally across your clusters using a distributed **Redis Cache**.
*   **Shadow Routing (Migration Auditor):** Duplicate live production prompts in background goroutines to test cheaper models side-by-side with zero reliability risk.
*   **Local PII Redaction Guard:** Automatically scrub credit cards, SSNs, and secrets on your local servers before prompts ever leave your private network.
*   **OIDC/Okta SSO Integration:** Satisfy procurement security audits out-of-the-box with Single Sign-On and dynamic Role-Based Access Controls.

---

## 🛠️ Performance & Overhead

Because AuraLLM is built in Go, it features near-zero overhead under high-concurrency mid-market workloads:

*   **RAM Footprint:** ~15MB at idle.
*   **Startup Latency:** less than 10ms.
*   **Edge deployment:** Runs as a lightweight, zero-dependency 20MB Docker container or Kubernetes sidecar.
