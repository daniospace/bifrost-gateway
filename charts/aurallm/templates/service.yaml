apiVersion: v1
kind: Service
metadata:
  name: {{ include "aurallm.fullname" . }}
  labels:
    {{- include "aurallm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aurallm.selectorLabels" . | nindent 4 }}