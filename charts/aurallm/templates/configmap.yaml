apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aurallm.fullname" . }}
  labels:
    {{- include "aurallm.labels" . | nindent 4 }}
data:
  PORT: {{ .Values.config.port | quote }}
  LOG_LEVEL: {{ .Values.config.logLevel | quote }}